// TODO: fill these in. Until then the links open WhatsApp and Instagram
// themselves, not FUEL's chat or profile.
/** Digits only, country code first, no "+" — e.g. "971501234567". */
const WHATSAPP_NUMBER = "";
/** Without the "@". */
const INSTAGRAM_HANDLE = "";

/** Where orders go: the hero's "Order yours" and the menu's WhatsApp link. */
export const WHATSAPP_URL = `https://wa.me/${WHATSAPP_NUMBER}`;
export const INSTAGRAM_URL = `https://www.instagram.com/${INSTAGRAM_HANDLE}`;
